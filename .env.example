# Build-time, all empty by default: preview is noindex, form prepares email locally.
SITE_ORIGIN=
PUBLIC_BASE_PATH=/
FORM_ENDPOINT=
PRIVACY_URL=
# Server environment, never commit real values. PHP hosting only.
FORM_ENABLED=0
FORM_PRIVACY_READY=0
FORM_ORIGIN=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Optional absolute directory outside the web root, writable by PHP.
FORM_RATE_DIR=
